<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

        {# Bootstrap link #}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">

        {% block stylesheets %}
            {# it's in public folder that we find all our public files like css/ js/ ... #}
            <link rel="styleSheet" href="{{asset('Css/style.css')}}">
            {# asset() -> Automatically goes & check the public folder #}
        {% endblock %}
    </head>

    <body>
        <div id="container">

            <nav class="navbar navbar-dark bg-dark">
                <a class="link" href="{{path('app_formation')}}"> List Of Formations </a><br>
                <a class="link" href="{{path('app_sessions')}}"> List Of Sessions </a><br>
                <a class="link" href="{{path('app_category')}}"> List Of Categories </a><br>
                <a class="link" href="{{path('app_module')}}"> List Of Modules </a><br>
                {# <a class="link" href="{{path('app_session_cours')}}"> List Of Programmes </a><br> #}
                <a class="link" href="{{path('app_intern')}}"> List Of Interns </a><br>

                {% if app.user %}
                    <a class="link" href="{{path('app_logout')}}"> Disconnect </a><br>
                    <a class="link" href="{{path('#')}}"> Profile </a>
                {% else %}
                    <a class="link" href="{{path('app_login')}}"> Connect </a><br>
                    <a class="link" href="{{path('app_register')}}"> Sign in </a><br>
                {% endif %}
            </nav>
            <br><hr><br>

            {% block body %}{% endblock %}

        </div>
    </body>
</html>
